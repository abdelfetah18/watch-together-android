<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/app_navigation"
    app:startDestination="@id/home_fragment">


    <fragment
        android:id="@+id/home_fragment"
        android:name="com.abdelfetahdev.watch_together.fragements.HomeFragment"
        android:label="Home"
        app:route="Home"
        tools:layout="@layout/fragment_home" />

    <fragment
        android:id="@+id/room_fragment"
        android:name="com.abdelfetahdev.watch_together.fragements.ProfileFragment"
        android:label="Room"
        app:route="Room"
        tools:layout="@layout/fragment_profile" />

    <fragment
        android:id="@+id/settings_fragment"
        android:name="com.abdelfetahdev.watch_together.fragements.SettingsFragment"
        android:label="Settings"
        app:route="Settings"
        tools:layout="@layout/fragment_settings" />

</navigation>